[project]
name = "esm2-scorer"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
esm2-score = "esm2_scorer.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
